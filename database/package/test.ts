import { UserSession, userTokenValidation } from './lib/esm/package';

const t: UserSession = {} as UserSession;
t.streamer?.page.affixUrl;

userTokenValidation.createFormParse({ name: 'anq', symbol: 'ANQ' });
